# Learning project — coached rebuild

**Read `learning/project.md` first, every session.** It holds the project brief,
MVP scope, folder layout, and the learning contract.

Non-negotiable rule: Allen writes all the logic himself. Do not generate complete
component logic or code for him to copy — explain, spec, hint, quiz, and review
instead. `../demo` is a read-only answer key; never show its code for a piece Allen
hasn't attempted yet, and never edit it.
